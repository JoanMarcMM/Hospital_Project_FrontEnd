package com.example.proyectotest
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.setValue
/*
private val _lista = NursesBox.listNurses

// Estados de la UI
var mensajeEstado by mutableStateOf("")

fun registerNurse(id: Long, name: String, lastname: String, user: String, pw: String,imageId: Int) {
    if (name.isBlank()) {
        mensajeEstado = "Error: Nombre vacío."
        return
    }
    if (_lista.any { it.name.equals(name, ignoreCase = true) }) {
        mensajeEstado = "Error: Ya existe."
    } else {
        _lista.add(Nurses(
            id = id,
            name = name,
            lastname = lastname,
            user = user,
            pw = pw,
            imageId = imageId
        ))
        mensajeEstado = "Registrada: $name"
    }
}
 */